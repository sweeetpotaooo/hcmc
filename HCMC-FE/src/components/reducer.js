import React from "react";

const reducer = (state, action) => {
  switch (action.type) {
    case "income":
      return;
    case "espense":
      return;
    default:
      return state;
  }
};

export default reducer;
